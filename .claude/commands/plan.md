---
description: Enter Plan Mode with a Revere-specific preamble
---

Enter Plan Mode. Before planning, re-read @revere-prd.md §7 (product spec)
and §12 (architecture). Then plan the requested change. After the plan is
approved, write it to `docs/plans/<slug>.md` so future sessions can resume.
